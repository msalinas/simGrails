Proyecto Grails SIM

-----------
You can use the generate-i18n-messages script to ouput all needed messages for a domain class to your command line interface.
grails generate-i18n-messages <domainClass>
-----------
//Pantallas para buscar y crear roles a nivel base de datos
http://localhost:8080/sim/requestmap/search
http://localhost:8080/sim/requestmap/create
-----------
//Ejemplo para que un campo sea igual a un dato, código debe ser colocado en la clase dominio

	SimCatTipoIdentificador claveIdentificador

	static constraints = {
		claveIdentificador(validator: { cveIdentificador ->
			cveIdentificador.claveTipoIdentificador.equals "REGIONAL" })
	}
----------
